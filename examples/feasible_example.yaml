# Model
model:
  time_horizon: 2
  dt: 1

  vars:
    state: [x]
    input: [u]

  bounds:
    x: (0, 100)
    u: (0, 1)

# Specification
    
init:
  - stl: x = 0


sys:
  - name: FooBar
    stl: "F[0,2](x > 5)"
    pri: 1

dyn:
  - stl: G[0, 2](x + -1*x' + dt*5*u = 0)
